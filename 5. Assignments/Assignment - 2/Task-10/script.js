// Task 10: Bus number route (even/odd)
let busNumber = 42; // You can change this value

// Check if bus number is even or odd
if (busNumber % 2 === 0) {
    console.log(`Bus number ${busNumber} goes to Route A (even)`);
} else {
    console.log(`Bus number ${busNumber} goes to Route B (odd)`);
}

